<!doctype html>
<html lang="is">

<head>
  <meta charset="utf-8">
  <title><%= locals.title ?? '' %></title>
  <link rel="stylesheet" href="/styles.css">
</head>

<body>
  <div class="container">
    <div class="offset-3 col col-6">

  <main>

    <h1><%= locals.title ?? '' %></h1>

    <% if (locals.loggedIn) { %>
      <p>Innsrkáður notandi: <%= locals.user.name %> (<%= locals.user.username %>)
    <% } else { %>
      <p>Þú ert ekki innskráður notandi.</p>
    <% } %>

    <ul>
      <li><a href="/">Forsíða</a></li>
      <li><a href="/leikir">Leikir</a></li>
      <li><a href="/stada">Staða</a></li>
      <li><a href="/thing">Thing</a></li>
    </ul>
